import "./Build.css"
import Button from './../Button/Button.js'

function Build() {
    return (
        <div className="Build">
            <p>Build:</p>
            <Button text="Back" />
            <Button text="Reset" />
        </div>
    )
}

export default Build;